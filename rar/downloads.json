{
  "_note": "Hand-maintained, per the approved pattern: download counts cannot be harvested automatically (the upstream repo maintains theirs from a Microsoft Clarity dashboard CSV, out of band). Refresh by overwriting this file; unknown slugs read as 0. Client-side telemetry (Clarity) is deliberately NOT enabled on AIBAST pages until a privacy notice ships — the upstream repo carries that tag without one, and that gap is recorded, not inherited."
}
