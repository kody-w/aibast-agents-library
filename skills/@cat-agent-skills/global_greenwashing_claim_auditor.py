"""
Global Greenwashing Claim Auditor — RAPP agent, mirrored from global_greenwashing_claim_auditor.md

This file is GENERATED from its skill.md by scripts/export_agent.py and is one
half of a matched pair. The manifest below and the procedure embedded further
down are identical to the ones in the .md; the only difference is the runtime:
a model reads the .md, a brainstem registers this .py and calls it as a tool.

Do not edit this file. Edit global_greenwashing_claim_auditor.md and re-run the exporter — the gate
re-derives this file and fails if the two have drifted apart.

Redistributed from CAT Agent Skills under MIT
with attribution. Original author: Chris Garty.
Origin: https://microsoft.github.io/cat-agent-skills/#global-greenwashing-claim-auditor
"""

# ===============================================================
# RAPP AGENT MANIFEST - Do not remove. Used by registry builder.
# ===============================================================
__manifest__ = {
    "schema": "rapp-agent/1.0",
    "name": "@cat-agent-skills/global_greenwashing_claim_auditor",
    "version": "1.0.0",
    "display_name": "Global Greenwashing Claim Auditor",
    "description": "Returns the verified RAPP procedure for: Audit environmental claims in CSV, XLSX, DOCX, PPTX, conversational text, and public websites with separate ANY, CA, EU, and UK findings. Call this to obtain the full step-by-step contract (inputs, outputs, verification) and then carry out those steps.",
    "author": "Chris Garty",
    "tags": [
        "greenwashing",
        "environmental claims",
        "sustainability",
        "compliance",
        "canada",
        "european union",
        "united kingdom",
        "documents"
    ],
    "category": "general",
    "quality_tier": "aggregated",
    "requires_env": [],
    "dependencies": [
        "@rapp/basic-agent"
    ],
    "mirrors": "global_greenwashing_claim_auditor.md",
    "source_url": "https://microsoft.github.io/cat-agent-skills/#global-greenwashing-claim-auditor",
    "source_license": "MIT",
    "converted_from": "CAT Agent Skills"
}
# ===============================================================

try:  # cloud layout (agents package)
    from agents.basic_agent import BasicAgent
except ImportError:  # local brainstem / standalone layout
    import os
    import sys

    sys.path.insert(0, os.path.join(
        os.path.dirname(os.path.abspath(__file__)), "..", "..",
        "agents", "@aibast-agents-library", "templates"))
    from basic_agent import BasicAgent


# The skill body, embedded verbatim. This is what makes the export single-file:
# the procedure travels with the agent rather than being fetched at call time.
SKILL_PROCEDURE = """# Global Greenwashing Claim Auditor

> **Converted skill.** This is a RAPP single-file skill converted from
> **CAT Agent Skills** ([origin](https://microsoft.github.io/cat-agent-skills/#global-greenwashing-claim-auditor)), redistributed under
> **MIT** with attribution. Original author: Chris Garty.
> Upstream license text: https://raw.githubusercontent.com/microsoft/cat-agent-skills/main/LICENSE
>
> Drop this file into your brainstem's skills folder, or read it and run the
> steps yourself. Everything the skill needs is in this one file.

## When to use this

Use this skill whenever the user asks to audit, scan, review, classify, or de-risk environmental, sustainability, climate, circularity, recycling, emissions, or green marketing claims in pasted conversational content, CSV files, XLSX workbooks, DOCX documents, PPTX presentations, or website URLs. Report universal vocabulary risks separately from Canadian (CA), European Union (EU), and United Kingdom (UK) legal and guidance findings.

## The deterministic layer

RAPP skills state their contract explicitly, so two runs of the same skill do
the same thing:

- **Inputs** — whatever the steps below name. If an input is missing, say so
  and stop rather than guessing.
- **Outputs** — the artifact the steps produce, named where it is written.
- **Verification** — before reporting success, confirm the output exists and
  matches what was asked. A silent partial result is a failure.
- **Configuration** — never hardcode an endpoint, key, or tenant. Read them
  from the environment (`requires_env` above lists what this skill needs).

## Skill

# Global Greenwashing Claim Auditor

Audit public-facing environmental claims using the bundled global vocabulary, regional taxonomy, legal research, and detection method. Treat the result as compliance triage, not legal advice.

## Required references

Read these before making final findings:

1. `references/Greenwashing-vocab.md`
2. `references/greenwashing-risk-taxonomy.md`
3. `references/greenwashing-detection.md`
4. `references/greenwashing-analysis-research.md`

Use `ANY` only for universal vocabulary and claim-construction risks. Use `CA`, `EU`, and `UK` only when the corresponding regional rule, law, regulation, or guidance supports the finding.

## Workflow

1. Determine the input type and intended markets. If markets are unknown, analyze `ANY`, `CA`, `EU`, and `UK`, but mark regional applicability as unconfirmed.
2. Extract all claim-bearing text while preserving source locations:
   - CSV: row and column.
   - XLSX: sheet and cell.
   - DOCX: paragraph or table cell.
   - PPTX: slide and shape.
   - Conversation: supplied text block.
   - URL: page URL and extracted block.
3. Run the matching script for first-pass triage.
4. Review every flagged unit against the full references. Keyword matches are leads, not verdicts.
5. Add claims missed by keywords, including implied claims, imagery, omissions, comparisons, labels, scope overreach, lifecycle issues, and unsupported future commitments.
6. Report separate findings for every applicable jurisdiction and retain the highest risk as the overall rating.
7. Suggest a bounded rewrite, but never invent evidence, percentages, certifications, methods, or environmental benefits.

## Scripts

Run scripts from the skill directory with Python 3. Use workspace-relative paths so commands remain portable across supported environments.

### CSV

```sh
python scripts/analyze_csv.py "claims.csv" --jurisdictions CA EU UK --out audit.json
```

Use `--text-column NAME` repeatedly to choose columns. Without it, the script scans text-like columns and then all string columns if necessary.

### XLSX

```sh
python scripts/analyze_xlsx.py "claims.xlsx" --sheet "Posts" --jurisdictions CA EU UK --out audit.json
```

Use `--max-items 1000` for a bounded first pass.

### DOCX

```sh
python scripts/analyze_docx.py "document.docx" --jurisdictions CA EU UK --out audit.json
```

### PPTX

```sh
python scripts/analyze_pptx.py "presentation.pptx" --jurisdictions CA EU UK --out audit.json
```

### Conversational content

For short content, analyze it directly using the references. For repeatable scripted triage:

```sh
python scripts/analyze_text.py --text "Our product is carbon neutral and eco-friendly." --jurisdictions CA EU UK --out audit.json
```

For long content:

```sh
python scripts/analyze_text.py --text-file "content.txt" --jurisdictions CA EU UK --out audit.json
```

### Website URL

```sh
python scripts/analyze_url.py "https://example.com/product" --jurisdictions CA EU UK --out audit.json
```

Only fetch public HTTP/HTTPS pages. Do not attempt to bypass authentication, paywalls, robots controls, or access restrictions.

### Markdown output

All entry scripts accept `--format markdown`:

```sh
python scripts/analyze_docx.py "document.docx" --format markdown --out audit.md
```

## Review requirements

Always manually inspect:

- Red findings.
- Generic claims such as `green`, `eco-friendly`, and `sustainable`.
- Carbon-neutral, climate-neutral, net-zero, or offset claims.
- Sustainability labels, seals, certifications, and badges.
- Recyclable, compostable, biodegradable, reusable, and circularity claims.
- Comparisons and future targets.
- Qualifications in footnotes, small print, links, notes, or other slides/pages.

Apply date-sensitive EU rules using the content's intended publication date. If no date is supplied, use the current date and state that assumption.

## Output

Return:

1. Scope, sources, assumed publication date, and applicable jurisdictions.
2. Counts by overall risk and jurisdiction tag.
3. One finding per claim and region, including exact quote, source location, finding code, risk, basis, reason, and remediation.
4. Highest-priority fixes.
5. Limitations, inaccessible evidence, and items requiring legal or technical review.

## Guardrails

- Do not describe `ANY` as a legal violation.
- Do not declare legal compliance; state that no issue was detected under the checks performed.
- Do not treat a keyword match alone as proof of greenwashing.
- Do not invent or assume substantiation.
- Do not ignore visual or contextual implications merely because extracted text appears qualified.
- Do not apply withdrawn proposed legislation as enacted law.
- Escalate high-risk claims for qualified legal review before publication.

## Tone

Be precise, evidence-led, constructive, and explicit about jurisdiction. Every Yellow or Red finding should include a practical remediation.

---

*Converted for the AIBAST Agents Library from CAT Agent Skills.
The original is authoritative; this file adds the RAPP manifest and the
deterministic layer above, and changes nothing else about the instructions.*
"""

SKILL_REF = "@cat-agent-skills/global_greenwashing_claim_auditor"
SKILL_LICENSE = "MIT"
SKILL_SOURCE = "https://microsoft.github.io/cat-agent-skills/#global-greenwashing-claim-auditor"


class GlobalGreenwashingClaimAuditorAgent(BasicAgent):
    """Delivers a verified RAPP procedure as a callable tool.

    Inputs — none required. `context` optionally narrows the returned
        procedure to a stated situation; it is echoed back, never invented.
    Output — the full skill procedure as text: inputs, outputs, and the
        verification step the caller must perform before reporting success.
    Verification — the caller confirms the artifact named by the procedure
        exists and matches what was asked. This agent returns instructions; it
        does not perform the work described in them, and it never reports
        success on the caller's behalf.
    Configuration — none. The procedure is embedded in this file.
    """

    def __init__(self):
        self.name = "Global Greenwashing Claim Auditor"
        self.metadata = {
            "name": self.name,
            "description": __manifest__["description"],
            "parameters": {
                "type": "object",
                "properties": {
                    "context": {
                        "type": "string",
                        "description": (
                            "Optional. The specific situation the procedure is "
                            "being applied to, echoed back with the steps — for "
                            "example 'a 40-slide deck for an external customer'."
                        ),
                    }
                },
                "required": [],
            },
        }
        super().__init__(name=self.name, metadata=self.metadata)

    def perform(self, **kwargs):
        context = (kwargs.get("context") or "").strip()
        header = [
            f"# {self.name} — RAPP procedure",
            "",
            f"Source: {SKILL_REF} · redistributed under {SKILL_LICENSE} "
            f"with attribution ({SKILL_SOURCE}).",
        ]
        if context:
            header += ["", f"**Applied to:** {context}"]
        header += [
            "",
            "Follow the steps below exactly. Where a step names an input you "
            "do not have, say so and stop rather than guessing, and complete "
            "the verification step before reporting success.",
            "",
            "---",
            "",
        ]
        return "\n".join(header) + SKILL_PROCEDURE
