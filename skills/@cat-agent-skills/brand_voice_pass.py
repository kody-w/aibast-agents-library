"""
Brand Voice Pass — RAPP agent, mirrored from brand_voice_pass.md

This file is GENERATED from its skill.md by scripts/export_agent.py and is one
half of a matched pair. The manifest below and the procedure embedded further
down are identical to the ones in the .md; the only difference is the runtime:
a model reads the .md, a brainstem registers this .py and calls it as a tool.

Do not edit this file. Edit brand_voice_pass.md and re-run the exporter — the gate
re-derives this file and fails if the two have drifted apart.

Redistributed from CAT Agent Skills under MIT
with attribution. Original author: Simon Owen.
Origin: https://microsoft.github.io/cat-agent-skills/#brand-voice-pass
"""

# ===============================================================
# RAPP AGENT MANIFEST - Do not remove. Used by registry builder.
# ===============================================================
__manifest__ = {
    "schema": "rapp-agent/1.0",
    "name": "@cat-agent-skills/brand_voice_pass",
    "version": "0.1.0",
    "display_name": "Brand Voice Pass",
    "description": "Returns the verified RAPP procedure for: Rewrite drafts into a configurable house style, preserving meaning while removing generic AI phrasing. Call this to obtain the full step-by-step contract (inputs, outputs, verification) and then carry out those steps.",
    "author": "Simon Owen",
    "tags": [
        "writing",
        "content",
        "voice",
        "authoring",
        "productivity"
    ],
    "category": "it_management",
    "quality_tier": "aggregated",
    "requires_env": [],
    "dependencies": [
        "@rapp/basic-agent"
    ],
    "mirrors": "brand_voice_pass.md",
    "source_url": "https://microsoft.github.io/cat-agent-skills/#brand-voice-pass",
    "source_license": "MIT",
    "converted_from": "CAT Agent Skills"
}
# ===============================================================

try:  # cloud layout (agents package)
    from agents.basic_agent import BasicAgent
except ImportError:  # local brainstem / standalone layout
    import os
    import sys

    sys.path.insert(0, os.path.join(
        os.path.dirname(os.path.abspath(__file__)), "..", "..",
        "agents", "@aibast-agents-library", "templates"))
    from basic_agent import BasicAgent


# The skill body, embedded verbatim. This is what makes the export single-file:
# the procedure travels with the agent rather than being fetched at call time.
SKILL_PROCEDURE = """# Brand Voice Pass

> **Converted skill.** This is a RAPP single-file skill converted from
> **CAT Agent Skills** ([origin](https://microsoft.github.io/cat-agent-skills/#brand-voice-pass)), redistributed under
> **MIT** with attribution. Original author: Simon Owen.
> Upstream license text: https://raw.githubusercontent.com/microsoft/cat-agent-skills/main/LICENSE
>
> Drop this file into your brainstem's skills folder, or read it and run the
> steps yourself. Everything the skill needs is in this one file.

## When to use this

Use this skill when the user asks to rewrite, humanise, de-AI, polish, localise, or align draft text to a brand voice, house style, named audience, or writing profile.

## The deterministic layer

RAPP skills state their contract explicitly, so two runs of the same skill do
the same thing:

- **Inputs** — whatever the steps below name. If an input is missing, say so
  and stop rather than guessing.
- **Outputs** — the artifact the steps produce, named where it is written.
- **Verification** — before reporting success, confirm the output exists and
  matches what was asked. A silent partial result is a failure.
- **Configuration** — never hardcode an endpoint, key, or tenant. Read them
  from the environment (`requires_env` above lists what this skill needs).

## Skill

# Brand Voice Pass

Use this skill to transform draft copy into a defined brand or personal voice while preserving the user's meaning, evidence, terminology, and intent.

## Operating principles

1. **Preserve the truth.** Do not invent facts, proof points, dates, clients, figures, quotes, or outcomes.
2. **Preserve the argument.** Improve expression without changing the user's position unless they explicitly ask for a stronger or softer stance.
3. **Remove generic AI tells.** Avoid empty intensifiers, inflated claims, tidy-but-bland phrasing, excessive signposting, and repetitive short sentences.
4. **Use the requested variant of English.** Default to the user's language and regional spelling. If unspecified and the surrounding material uses UK English, use UK English.
5. **Respect the destination.** Adjust length, cadence, paragraphing, and directness for the intended channel.
6. **Keep it usable.** Return the rewritten copy first. Add notes only when useful.

## Inputs to look for

- Draft text or notes to rewrite.
- Brand or personal voice guidance.
- Audience, channel, objective, and desired length.
- Words or phrases to avoid.
- Required terminology, source facts, or claims that must remain intact.

If the user has not supplied a full voice guide, infer only light stylistic choices from the draft and clearly avoid claiming a detailed brand profile exists.

## Workflow

1. Read the full draft before editing.
2. Identify the core message, supporting evidence, and intended action.
3. Apply the voice rules without adding unsupported content.
4. Improve flow, transitions, sentence rhythm, and paragraph shape.
5. Remove generic AI phrasing and replace it with concrete, human language.
6. Check the output against the original for meaning drift.
7. Return the final copy in a copy-ready format.

## Default voice controls

Use these controls unless the user provides a different profile:

- Prefer concrete language over abstract positioning.
- Prefer lived-experience phrasing over consultancy jargon.
- Use natural paragraphing rather than long blocks or a list of fragments.
- Avoid repetitive, overly neat sentence structures.
- Avoid claims such as "game-changing", "revolutionary", "unlock", "seamless", "in today's fast-paced world", and similar filler unless they are in source text and must be retained.
- Keep the user's level of confidence: do not over-polish into corporate blandness.

## Output patterns

For a simple rewrite, output only:

```markdown
[Rewritten copy]
```

For a rewrite where useful issues were found, output:

```markdown
[Rewritten copy]

Notes:
- [Short note on any assumption, ambiguity, or retained risk.]
```


## References

This skill includes supporting reference material. Read the relevant reference file when the task needs additional structure, rubric detail, examples, or checklist support.

- `references/voice-profile-template.md` - use this when additional structure, examples, or checks are useful for the task.

## Quality checklist

Before responding, check:

- The rewritten text keeps the original meaning.
- No new evidence or claims have been invented.
- The copy sounds like a person, not a generic assistant.
- The requested language variant and channel fit are respected.
- The result is ready for the user to paste or publish.

---

*Converted for the AIBAST Agents Library from CAT Agent Skills.
The original is authoritative; this file adds the RAPP manifest and the
deterministic layer above, and changes nothing else about the instructions.*
"""

SKILL_REF = "@cat-agent-skills/brand_voice_pass"
SKILL_LICENSE = "MIT"
SKILL_SOURCE = "https://microsoft.github.io/cat-agent-skills/#brand-voice-pass"


class BrandVoicePassAgent(BasicAgent):
    """Delivers a verified RAPP procedure as a callable tool.

    Inputs — none required. `context` optionally narrows the returned
        procedure to a stated situation; it is echoed back, never invented.
    Output — the full skill procedure as text: inputs, outputs, and the
        verification step the caller must perform before reporting success.
    Verification — the caller confirms the artifact named by the procedure
        exists and matches what was asked. This agent returns instructions; it
        does not perform the work described in them, and it never reports
        success on the caller's behalf.
    Configuration — none. The procedure is embedded in this file.
    """

    def __init__(self):
        self.name = "Brand Voice Pass"
        self.metadata = {
            "name": self.name,
            "description": __manifest__["description"],
            "parameters": {
                "type": "object",
                "properties": {
                    "context": {
                        "type": "string",
                        "description": (
                            "Optional. The specific situation the procedure is "
                            "being applied to, echoed back with the steps — for "
                            "example 'a 40-slide deck for an external customer'."
                        ),
                    }
                },
                "required": [],
            },
        }
        super().__init__(name=self.name, metadata=self.metadata)

    def perform(self, **kwargs):
        context = (kwargs.get("context") or "").strip()
        header = [
            f"# {self.name} — RAPP procedure",
            "",
            f"Source: {SKILL_REF} · redistributed under {SKILL_LICENSE} "
            f"with attribution ({SKILL_SOURCE}).",
        ]
        if context:
            header += ["", f"**Applied to:** {context}"]
        header += [
            "",
            "Follow the steps below exactly. Where a step names an input you "
            "do not have, say so and stop rather than guessing, and complete "
            "the verification step before reporting success.",
            "",
            "---",
            "",
        ]
        return "\n".join(header) + SKILL_PROCEDURE
