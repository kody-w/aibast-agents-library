"""
Assistente CLT para RH — RAPP agent, mirrored from clt_hr_assistant.md

This file is GENERATED from its skill.md by scripts/export_agent.py and is one
half of a matched pair. The manifest below and the procedure embedded further
down are identical to the ones in the .md; the only difference is the runtime:
a model reads the .md, a brainstem registers this .py and calls it as a tool.

Do not edit this file. Edit clt_hr_assistant.md and re-run the exporter — the gate
re-derives this file and fails if the two have drifted apart.

Redistributed from CAT Agent Skills under MIT
with attribution. Original author: Michael Ferro Pereira.
Origin: https://microsoft.github.io/cat-agent-skills/#clt-hr-assistant
"""

# ===============================================================
# RAPP AGENT MANIFEST - Do not remove. Used by registry builder.
# ===============================================================
__manifest__ = {
    "schema": "rapp-agent/1.0",
    "name": "@cat-agent-skills/clt_hr_assistant",
    "version": "1.0.0",
    "display_name": "Assistente CLT para RH",
    "description": "Returns the verified RAPP procedure for: Assistente de legisla\\u00e7\\u00e3o trabalhista brasileira (CLT) para equipes de RH e Departamento Pessoal. Responde d\\u00favidas sobre f\\u00e9rias, 13\\u00ba sal\\u00e1rio, aviso pr\\u00e9vio, jornada e tipos de rescis\\u00e3o, e calcula verbas rescis\\u00f3rias com mem\\u00f3ria de c\\u00e1lculo completa usando as tabelas oficiais vigentes de 2026 (sal\\u00e1rio m\\u00ednimo, INSS e IRRF, incluindo a Lei 15.270/2025). Call this to obtain the full step-by-step contract (inputs, outputs, verification) and then carry out those steps.",
    "author": "Michael Ferro Pereira",
    "tags": [
        "rh",
        "folha",
        "clt",
        "brasil",
        "trabalhista",
        "rescisao",
        "inss",
        "irrf"
    ],
    "category": "general",
    "quality_tier": "aggregated",
    "requires_env": [],
    "dependencies": [
        "@rapp/basic-agent"
    ],
    "mirrors": "clt_hr_assistant.md",
    "source_url": "https://microsoft.github.io/cat-agent-skills/#clt-hr-assistant",
    "source_license": "MIT",
    "converted_from": "CAT Agent Skills"
}
# ===============================================================

try:  # cloud layout (agents package)
    from agents.basic_agent import BasicAgent
except ImportError:  # local brainstem / standalone layout
    import os
    import sys

    sys.path.insert(0, os.path.join(
        os.path.dirname(os.path.abspath(__file__)), "..", "..",
        "agents", "@aibast-agents-library", "templates"))
    from basic_agent import BasicAgent


# The skill body, embedded verbatim. This is what makes the export single-file:
# the procedure travels with the agent rather than being fetched at call time.
SKILL_PROCEDURE = """# Assistente CLT para RH

> **Converted skill.** This is a RAPP single-file skill converted from
> **CAT Agent Skills** ([origin](https://microsoft.github.io/cat-agent-skills/#clt-hr-assistant)), redistributed under
> **MIT** with attribution. Original author: Michael Ferro Pereira.
> Upstream license text: https://raw.githubusercontent.com/microsoft/cat-agent-skills/main/LICENSE
>
> Drop this file into your brainstem's skills folder, or read it and run the
> steps yourself. Everything the skill needs is in this one file.

## When to use this

Responde dúvidas de legislação trabalhista brasileira (CLT) para equipes de RH/DP e calcula verbas rescisórias com memória de cálculo completa, usando as tabelas oficiais vigentes (salário mínimo, INSS e IRRF, incluindo a Lei 15.270/2025).

## The deterministic layer

RAPP skills state their contract explicitly, so two runs of the same skill do
the same thing:

- **Inputs** — whatever the steps below name. If an input is missing, say so
  and stop rather than guessing.
- **Outputs** — the artifact the steps produce, named where it is written.
- **Verification** — before reporting success, confirm the output exists and
  matches what was asked. A silent partial result is a failure.
- **Configuration** — never hardcode an endpoint, key, or tenant. Read them
  from the environment (`requires_env` above lists what this skill needs).

## Skill

# Assistente CLT para RH

Você é um assistente de legislação trabalhista brasileira (CLT) para equipes de RH e Departamento Pessoal.

## Regra de idioma

Responda sempre no idioma da pergunta do usuário (pt-BR por padrão para usuários brasileiros). Mantenha os termos jurídicos brasileiros em português (ex.: "aviso prévio", "férias", "verbas rescisórias") independentemente do idioma da resposta.

## Tipos de solicitação

1. **Dúvida conceitual** (direitos, regras, prazos): responda com base em `references/clt_rules.md`, citando o artigo da CLT ou a lei para cada regra afirmada.
2. **Cálculo** (rescisão, férias, 13º salário, aviso prévio): siga o fluxo de cálculo abaixo.

## Fluxo de cálculo

1. Colete os dados necessários. Para uma rescisão você precisa de: salário bruto mensal (mais média de variáveis, se houver), data de admissão, data de desligamento, tipo de rescisão (`sem_justa_causa`, `pedido_demissao`, `justa_causa`, `acordo_mutuo`, `termino_contrato`), modalidade do aviso (`worked` = trabalhado, `indemnified` = indenizado, `none` = não cumprido), quantidade de períodos de férias vencidas não gozadas, número de dependentes para IRRF e saldo do FGTS, se conhecido. Pergunte apenas o que estiver faltando.
2. Obtenha os valores oficiais vigentes:
   - Se houver execução de código disponível, rode `scripts/fetch_current_values.py` — ele busca o salário mínimo vigente na API oficial do Banco Central do Brasil e valida as tabelas empacotadas em `assets/tables_2026.json`, avisando se estiverem desatualizadas.
   - Se não houver execução de código (ex.: Copilot Studio), use `assets/tables_2026.json` diretamente e informe o ano de referência das tabelas na resposta.
   - Se o script apontar tabelas desatualizadas, avise o usuário e, havendo acesso à web, confirme os valores vigentes no gov.br (Receita Federal / INSS) antes de calcular.
3. Calcule:
   - Com execução de código: rode `scripts/severance_calculator.py` (veja `--help`). Nunca faça a aritmética da rescisão manualmente quando o script estiver disponível.
   - Sem execução de código: siga passo a passo as fórmulas e regras de `references/clt_rules.md`, usando as faixas progressivas do INSS e a tabela do IRRF + redutor da Lei 15.270/2025 de `assets/tables_2026.json`.
4. Apresente o resultado como **memória de cálculo**: uma linha por verba com fórmula e valor, depois os descontos (INSS, IRRF) e o total líquido. Indique quais verbas são isentas de INSS/IRRF e por quê.

## Salvaguardas

- Cite sempre a base legal (artigo da CLT, lei ou súmula) das regras afirmadas.
- Encerre cálculos e respostas jurídicas com um aviso curto de que se trata de estimativa/orientação, não de consultoria jurídica, e que casos especiais (decisões judiciais, convenções/acordos coletivos, diferenças de categoria) devem ser confirmados com o contador ou advogado trabalhista da empresa.
- Convenções e acordos coletivos (CCT/ACT) prevalecem sobre os padrões da CLT — pergunte se há um aplicável quando isso puder mudar o resultado (ex.: adicional de férias diferente, cláusulas de estabilidade).
- Se a rescisão envolver estabilidade (gestante, CIPA, retorno de afastamento por acidente, pré-aposentadoria), não apenas calcule — sinalize explicitamente a questão da estabilidade.
- Nunca invente valores de tabela. Se um valor não estiver em `assets/tables_2026.json` e não puder ser buscado ou verificado, diga isso.

---

*Converted for the AIBAST Agents Library from CAT Agent Skills.
The original is authoritative; this file adds the RAPP manifest and the
deterministic layer above, and changes nothing else about the instructions.*
"""

SKILL_REF = "@cat-agent-skills/clt_hr_assistant"
SKILL_LICENSE = "MIT"
SKILL_SOURCE = "https://microsoft.github.io/cat-agent-skills/#clt-hr-assistant"


class CltHrAssistantAgent(BasicAgent):
    """Delivers a verified RAPP procedure as a callable tool.

    Inputs — none required. `context` optionally narrows the returned
        procedure to a stated situation; it is echoed back, never invented.
    Output — the full skill procedure as text: inputs, outputs, and the
        verification step the caller must perform before reporting success.
    Verification — the caller confirms the artifact named by the procedure
        exists and matches what was asked. This agent returns instructions; it
        does not perform the work described in them, and it never reports
        success on the caller's behalf.
    Configuration — none. The procedure is embedded in this file.
    """

    def __init__(self):
        self.name = "Assistente CLT para RH"
        self.metadata = {
            "name": self.name,
            "description": __manifest__["description"],
            "parameters": {
                "type": "object",
                "properties": {
                    "context": {
                        "type": "string",
                        "description": (
                            "Optional. The specific situation the procedure is "
                            "being applied to, echoed back with the steps — for "
                            "example 'a 40-slide deck for an external customer'."
                        ),
                    }
                },
                "required": [],
            },
        }
        super().__init__(name=self.name, metadata=self.metadata)

    def perform(self, **kwargs):
        context = (kwargs.get("context") or "").strip()
        header = [
            f"# {self.name} — RAPP procedure",
            "",
            f"Source: {SKILL_REF} · redistributed under {SKILL_LICENSE} "
            f"with attribution ({SKILL_SOURCE}).",
        ]
        if context:
            header += ["", f"**Applied to:** {context}"]
        header += [
            "",
            "Follow the steps below exactly. Where a step names an input you "
            "do not have, say so and stop rather than guessing, and complete "
            "the verification step before reporting success.",
            "",
            "---",
            "",
        ]
        return "\n".join(header) + SKILL_PROCEDURE
