{
  "schema": "https://github.com/microsoft/aibast-agents-library/frontier-toolchain-policy/v1",
  "publication_enabled": false,
  "required": {
    "electron": "43.6.0",
    "electron_builder": "26.16.0",
    "macos_minimum": "12.0"
  },
  "publication_blockers": [
    "electron 43.6.0 is not available from the configured npm package feed, so no reproducible package lock or npm ci validation can be produced.",
    "The upstream electron-builder@26.16.0 release and tag exist, but no installable electron-builder 26.16.0 package is available from the configured or public npm registry, so no reproducible package lock or npm ci validation can be produced."
  ]
}
