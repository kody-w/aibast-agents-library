# Transcripts

Raw discovery call transcripts, meeting recordings, and feedback sessions.

## What goes here

- `.txt` transcripts (input to `transcript_to_agent`)
- `.mp3`/`.mp4` recordings
- `.vtt`/`.srt` captions

## Tip

After processing a transcript through RAPP, move the outputs to `projects/<customer>/outputs/`
